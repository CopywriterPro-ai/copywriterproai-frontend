import { UserNavbar as Navbar } from "@components/common/Navbar";

const UserHeader = () => {
  return (
    <header>
      <Navbar />
      <h1>Header</h1>
    </header>
  );
};

export default UserHeader;
